#include <stdio.h>
int main() {
    int N;
    int i = 0;
    int payment;
    int currentStreak = 0;
    int longestStreak = 0;
    scanf("%d", &N);
    while (i < N) {
        scanf("%d", &payment);

        if (payment == 0) {
            currentStreak++;   
            if (currentStreak > longestStreak) {
                longestStreak = currentStreak;
            }
        } else {
            currentStreak = 0; 
        }

        i++;
    }
    printf("Longest Default Streak: %d", longestStreak);

    return 0;
}
    
