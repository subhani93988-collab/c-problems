class Solution {
    public boolean checkValidString(String s) {
        int open=0,close=0;
        char[] temp=s.toCharArray();
        for(char each:temp){
            if(each=='('){open++;close++;}
            else if(each==')'){
                if(open>0)open--;
                close--;
            }else{
                if(open>0)open--;
                close++;
            }
            if(close<0) return false;
        }
        return open==0;
    }
