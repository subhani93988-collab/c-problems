#include <stdio.h>
int main() {
    int marks;
    printf("enter the marks:");
    scanf("%d",&marks);
    if (marks>=40)
    printf("it is pass");
    else 
    printf("it is fail");
    
    return 0;
}